/* Top Logo */
.top-logo {
    max-width: 300px; /* Adjust if heartbeat.png is larger/smaller */
    height: auto;
}

/* Social Media Icons */
.social-media img {
    width: 40px; /* Adjust if your icons are different sizes */
    height: 40px;
}

/* Bottom Logo */
.bottom-logo {
    max-width: 200px; /* Adjust if flying-pig.png is larger/smaller */
    height: auto;
    margin-top: 20px;
}
